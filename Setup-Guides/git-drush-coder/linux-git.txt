#linux specifc
eval `ssh-agent` # will start the ssh agent
$ ssh-add ~/.ssh/joshua.graham.private # enter your password when prompted

$git bash
export PATH="/c/Program Files (x86)/DevDesktop/php5_5:$PATH"
drush updb -y && drush uli --uid=1 --uri=http://mysite.build.localtest.me"

#cmd
set path=%path%;C:/xampp/php

#phi.ini - windows xampp (note: use git drush coder instead - this is hacky)
include_path=.;C:\xampp\php\PEAR

#general
find docroot/sites/all/themes/my_theme/ -regex ".*\.\(php\|inc\)" | xargs .git/hooks/tasks/code-review.sh
